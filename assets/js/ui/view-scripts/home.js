define([
    '$'
],
function($) {
    var homeUI = function() {
        // $('.js-header-nav__toggle').on('click', function() {
        //     $('.c-main-menu').trigger('main-menu:toggle');
        // });
    };

    return homeUI;
});
